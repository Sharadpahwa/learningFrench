<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.learningfrench">

    <application


        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="learningFrench"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".favorite"></activity>
        <activity android:name=".watchVideo" />
        <activity android:name=".videoLearning" />
        <activity android:name=".feedBack" />
        <activity android:name=".newWord" />
        <activity android:name=".Game" />
        <activity android:name=".userHome" />
        <activity android:name=".quiz" />
        <activity android:name=".loginUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>